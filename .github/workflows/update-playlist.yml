name: Update Playlist

on:
schedule:
    - cron: '0 13 1-7 * 5'   # First Friday of the month, at 5:00 PM DXB time
    - cron: '0 13 15-21 * 5' # Third Friday of the month, at 5:00 PM DXB time

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run processor
        run: python check_playlist.py

      - name: Commit and push updated playlist
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ridoymovieserver2.m3u
          if git diff --staged --quiet; then
            echo "No changes – skipping commit."
          else
            git commit -m "Auto-update playlist · $(date -u '+%Y-%m-%d %H:%M UTC')"
            git push
          fi
